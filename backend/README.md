Lost & Found Backend

Run:

1. npm install
2. npm run dev  # starts nodemon on port 5000

API:
- GET /api/items
- GET /api/items/:id
- POST /api/items (form-data: name, description, image)

Images served from /uploads/<filename>
