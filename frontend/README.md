Backtrack Frontend

Run:

1. npm install
2. npm start  # runs vite on port 3000

Default login credentials:
- Student: student / 1234
- Admin: admin / admin123

The frontend expects the backend at http://localhost:5000
